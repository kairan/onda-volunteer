import { PlaceholderPage } from './placeholderPage';

export function MinistryLeadersPage() {
  return (
    <PlaceholderPage
      namespace="ministries"
      titleKey="delegation.title"
      bodyKey="delegation.body"
    />
  );
}
