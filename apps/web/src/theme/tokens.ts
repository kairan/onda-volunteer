/** Semantic CSS variable names (ADR 0001). Values live in `src/styles/globals.css`. */
export const REQUIRED_THEME_CSS_VARIABLES = [
  '--background',
  '--foreground',
  '--primary',
  '--primary-foreground',
  '--destructive',
  '--destructive-foreground',
  '--border',
  '--ring',
  '--radius',
] as const;
