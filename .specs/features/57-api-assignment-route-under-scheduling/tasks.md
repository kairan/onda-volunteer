# Tasks: API Assignment route under Scheduling (#57)

## Task list

- [x] T57-01: Move `POST /events/:eventId/assignments` to `AssignmentsController` (Scheduling module).
- [x] T57-02: Remove assignment create from `EventsController`; drop `SchedulingModule` import from `EventsModule`.
- [x] T57-03: Web roster writes continue using same path (Scheduling-owned).
- [x] T57-04: API e2e assign scenarios remain green with unchanged behavior.
