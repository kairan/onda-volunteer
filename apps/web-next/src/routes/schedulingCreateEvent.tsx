import { PlaceholderPage } from './placeholderPage';

export function SchedulingCreateEventPage() {
  return (
    <PlaceholderPage
      namespace="scheduling"
      titleKey="create.title"
      bodyKey="create.signInRequired"
    />
  );
}
